---
description: 当用户需要读取网页、抓取登录后页面内容或分析最终渲染 HTML 时，调用 deep-read-web，并把 stdout 当作最终页面 HTML 上下文。优先使用精简 exe；只有本机没有系统 Edge/Chrome 或精简包无法运行时，再提示切换到带 Chromium 的 full 包。
alwaysApply: false
---

# Deep Read Web

在以下场景使用这个规则：

- 用户要求读取某个网页的完整 HTML
- 用户要求分析一个可能需要登录的页面
- 页面会先跳到登录页，登录后再跳回目标内容页
- 需要消费“最终页面 HTML”，而不是只看初始响应

## 调用优先级

### 1. 优先 exe

如果仓库中存在：

```text
skills/deep-read-web/bin/deep_read.exe
```

优先执行：

```bash
skills/deep-read-web/bin/deep_read.exe --HTML_PAGE "<url>"
```

### 2. 回退 Python

如果 exe 不存在，则执行：

```bash
py -3 skills/deep-read-web/scripts/deep_read.py --HTML_PAGE "<url>"
```

如果 `py -3` 不可用，再尝试：

```bash
python skills/deep-read-web/scripts/deep_read.py --HTML_PAGE "<url>"
```

## small / full 原则

- 默认优先 small 思路，不主动要求下载 Chromium / Firefox
- 如果本机已有 Edge/Chrome，small 包通常足够
- 只有在 small 包提示缺少 Chromium 回退浏览器时，才提示下载 full 包

建议给用户的按需下载命令：

```powershell
.\tools\install_release_binary.ps1
```

或显式下载 full：

```powershell
.\tools\install_release_binary.ps1 -Flavor full
```

## 参数规则

- 默认不额外传 `--browser`
- 只有用户明确要求浏览器时，才加 `--browser <value>`
- 默认不额外改 `--auth-timeout`
- 只有用户明确要求更长/更短等待时间时，才加 `--auth-timeout <seconds>`

## 结果消费规则

- 把 `stdout` 视为最终 HTML
- 把 `stderr` 视为状态提示或错误信息
- 如果脚本提示需要登录，让用户直接在浏览器窗口中完成登录
- 不要求用户把密码、cookie、token、验证码等敏感信息发到聊天中

## 环境缺失时的处理

- 没有 exe 且没有 Python：优先建议安装发布版
- 源码模式缺少 Playwright：提示用户参考 README 安装依赖
- 精简包缺少 Chromium 回退浏览器：提示安装 full 包，不默认要求 Firefox
