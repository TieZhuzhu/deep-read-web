---
description: 当用户需要读取网页、抓取登录后页面内容或分析最终渲染 HTML 时，调用 deep-read-web，并把 stdout 当作最终页面 HTML 上下文。优先使用本地 exe；如果用户只有 skill 且没有 Python，则优先提示运行 skill 内的 scripts/install_binary.ps1，默认下载 small 包，不够再升级 full 包。
alwaysApply: false
---

# Deep Read Web

在以下场景使用这个规则：

- 用户要求读取某个网页的完整 HTML
- 用户要求分析一个可能需要登录的页面
- 页面会先跳到登录页，登录后再跳回目标内容页
- 需要消费“最终页面 HTML”，而不是只看初始响应

## 调用优先级

### 1. 优先 exe

如果 skill 目录中存在：

```text
bin/deep_read.exe
```

优先执行：

```bash
bin/deep_read.exe --HTML_PAGE "<url>"
```

### 2. 回退 Python

如果 exe 不存在，则执行：

```bash
py -3 scripts/deep_read.py --HTML_PAGE "<url>"
```

如果 `py -3` 不可用，再尝试：

```bash
python scripts/deep_read.py --HTML_PAGE "<url>"
```

## 没有 Python 时的处理

如果用户只有 skill、没有 Python、也没有 exe：

先提示运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install_binary.ps1
```

安装脚本默认逻辑：

- 有系统 Edge/Chrome：下载 small
- 没有系统 Edge/Chrome：下载 full

如果 small 包运行失败且提示缺少 Chromium 回退浏览器，再提示：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install_binary.ps1 -Flavor full
```

## 参数规则

- 默认不额外传 `--browser`
- 只有用户明确要求浏览器时，才加 `--browser <value>`
- 默认不额外改 `--auth-timeout`
- 只有用户明确要求更长/更短等待时间时，才加 `--auth-timeout <seconds>`

## 结果消费规则

- 把 `stdout` 视为最终 HTML
- 把 `stderr` 视为状态提示或错误信息
- 如果脚本提示需要登录，让用户直接在浏览器窗口中完成登录
- 不要求用户把密码、cookie、token、验证码等敏感信息发到聊天中
